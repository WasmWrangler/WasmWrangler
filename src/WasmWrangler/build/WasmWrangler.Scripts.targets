<Project>
  <Target Name="WasmInjectScripts">
    <PropertyGroup>
      <_ScriptsDirectory>$(MSBuildProjectDirectory)/$(WasmJsDirectory)</_ScriptsDirectory>
    </PropertyGroup>

    <ItemGroup>
      <_ScriptFiles Include="
        $(MSBuildThisFileDirectory)/js/*.d.ts;
        $(MSBuildThisFileDirectory)/js/*.js;
        $(MSBuildThisFileDirectory)/js/*.js.map" />
    </ItemGroup>

    <Copy
      SourceFiles="@(_ScriptFiles)"
      DestinationFolder="$(_ScriptsDirectory)"
      SkipUnchangedFiles="true" />
  </Target>
</Project>
